export const Status = Object.freeze({
    succeeded: 'succeeded',
    loading: 'laoding',
    error: 'error',
    idle: 'idle',
});

export const Menu = Object.freeze({
    all: 'all',
    favorites: 'favorites',
    archived: 'archived'
});